import type { History } from "history";
/**
 * This function is required to avoid any race conditions caused by asynchronous history updates.
 */
export declare const makeQueue: (history: History) => (cb: () => void) => void;
